# kerbe config for the flutter-mini fixture.
kerbe:
  planning_root: planning/slices
  timezone: Pacific/Auckland
  promise_sources:
    spec_globs: ["*.md"]
    plan_glob: "*PLAN*.md"
  design:
    adapter: none
  stack:
    adapter: flutter
    code_roots: ["lib/", "pubspec.yaml"]
